[[{]]
## vim ALE (LSP)
- support fixing code in non-blocking manner with :ALEFix
- optional LSP with next features: (can be disabled to use CoC in place)
  - Diagnostics         (via LSP linters)
  - Go To Definition    (:ALEGoToDefinition)
  - Completion
  - Finding references  (:ALEFindReferences)
  - Hover information   (:ALEHover)
  - Symbol search       (:ALESymbolSearch)

- Linting Ussage:
  By default, linters will (re-)run in the background.

  Note: 'b:' (Buffer-local vars) always override 'g:' (global) settings.

  - job-control-Functions need to be configured like:
    ~/.vim/ftplugin/javascript.vim (recommended, by buffer 'type?', notice the b: prefix)
                    └─────┬──────┘
    ┌─────────────────────┴───────────────────┐
    ...
                                                      By default. all available tools for all
                                                      supported languages will be run. Run only explicit linters with:
                                                      let g:ale_linters_explicit = 1   (in ~/.vimrc)
  + let b:ale_linters = ['eslint']                <·· alt 1: Enable only 'eslint'
  + let b:ale_linters = {'javascript': ['eslint']}<·· alt 2: == alt1 (since we are in 'javascript.vim')
    ...                                               alt 3: Enable globably in ~/.vimrc before|after plugin load
                                                      + let g:ale_linters = {
                                                      + \   'javascript': ['eslint'],
                                                      + \}
  + " Fix files with prettier, and then ESLint.
                        ┌──────────────────┬─········ functions, including lambda also accepted.
                        v                  v
  + let b:ale_fixers = ['prettier', 'eslint']     <·· alt 1: plain List not allowed in global .vimrc
  + let b:ale_fixers = {                          <·· alt 2:
  + \    'javascript': ['prettier', 'eslint']         A * in place of the filetype will apply a
  + \}                                                List of fixers to all files which do not match
                                                      some filetype in the Dictionary.
                                                      alt 3: Enable globably in ~/.vimrc before|after plugin load
                                                        ...
                                                      + let g:ale_fixers = {
                                                      + \  '*': ['remove_trailing_lines', 'trim_whitespace'],
                                                      + \  'javascript': ['eslint'],
                                                      + \}

    ~/.vim/ftplugin/python.vim (recommended, by buffer 'type?', notice the b: prefix)
                    └───┬────┘
    ┌───────────────────┴───────────────────────┐
  + " Check Python files with flake8 and pylint.
  + let b:ale_linters = ['flake8', 'pylint']
  + " Fix Python files with autopep8 and yapf.
  + let b:ale_fixers = ['autopep8', 'yapf']
  + " Disable warnings about trailing whitespace
  + let b:ale_warn_about_trailing_whitespace = 0


  ~/.vimrc
  + " ALE: Set to 1 to fix files when saving
  + let g:ale_fix_on_save = 1
  + " keep ALE 'sign gutter' (left colum) open at all times
  + g:ale_sign_column_always to 1
  + " customize ALE text used for signs:
  + let g:ale_sign_error = 'EE'
  + let g:ale_sign_warning = 'WW'
  + " ALE: Customize colors ....
  + highlight clear ALEErrorSign
  + highlight clear ALEWarningSign
  + highlight ALEWarning ctermbg=DarkMagenta
  + " ALE: ... or Completely disable highlighting
  + let g:ale_set_highlights = 0
  +
  + " disable while typing (save battery/CPU)
  + g:ale_lint_on_text_changed = never
  + " Save more CPU/battery trigger only with :ALELint
  + g:ale_lint_on_enter
  +
  + " Show 5 lines of errors (default: 10)
  + let g:ale_list_window_size = 5
  +
  + " disable floating-box border ...
  + let g:ale_floating_window_border = []
  + " ... or customize it:
  + let g:ale_floating_window_border = ['│', '─', '╭', '╮', '╯', '╰', '│', '─']
  +

- Completion: (Refer to original source for config, CoC preferred)

5.vi. How can I change or disable the highlights ALE uses?

ALE's highlights problems with highlight groups which link to SpellBad, SpellCap, error, and todo groups by default. The characters that are highlighted depend on the linters being used, and the information provided to ALE.



- How to implement custom-made statusline with errors/warnings?
         (vs using vim-airline -python based)
  ALE provides some functions to assist on it:
  - ale#statusline#Count: returns number-of-problems found for a specified buffer.
  - ale#statusline#FirstProblem: returns dictionary containing full loclist-details
    of  1st problem of a specified type found in a buffer.
    (e.g. The first style warning in the current buffer.)
    eg: display all errors as one figure, and all non-errors as another figure:
    function! LinterStatus() abort
        let l:counts = ale#statusline#Count(bufnr(''))

        let l:all_errors = l:counts.error + l:counts.style_error
        let l:all_non_errors = l:counts.total - l:all_errors

        return l:counts.total == 0 ? 'OK' : printf(
        \   '%dW %dE',
        \   all_non_errors,
        \   all_errors
        \)
    endfunction

    set statusline=%{LinterStatus()}

- ALE life-cycle hooks:

  augroup YourGroup
      autocmd!
      autocmd User ALELintPre    call YourFunction()
      autocmd User ALELintPost   call YourFunction()

      autocmd User ALEJobStarted call YourFunction()

      autocmd User ALEFixPre     call YourFunction()
      autocmd User ALEFixPost    call YourFunction()
  augroup END

- Saving battery:
  - increase the delay before which ALE will run any linters while you type.

- Configuring C/C-- projects:
  - PROBLEM: many different build tools/project formats.
    ALE cannot easily detect which compiler flags to use, but it can
    attempt to parse "standarized" 'compile_commands.json for this
    purpose. (See Clang's documentation for more info. Note: CMake
    allows to easely create this file)
    - Some tools build-tools can understand this file too:
      - 'cppcheck', 'clangcheck', 'clangtidy', 'cquery' linters can read
    - Others like 'gcc', 'clang', .. can NOT and they will need to tell
      ALE which compiler flags to use. Eg:
      g:ale_pattern_options = ...
      b:ale_linters = ...

    - g:ale_c_parse_makefile can be setup to automatically run "make -n"
      (dry runs on Makefiles) in order to discover compiler flags.
      WARN: This can execute arbitrary code, disabled by default.

For configuring files based on regular expression patterns matched against the absolute path to a file, you can use g:ale_pattern_options.
" Do not lint or fix minified files.
let g:ale_pattern_options = {
\ '\.min\.js$': {'ale_linters': [], 'ale_fixers': []},
\ '\.min\.css$': {'ale_linters': [], 'ale_fixers': []},
\}
" If you configure g:ale_pattern_options outside of vimrc, you need this.
let g:ale_pattern_options_enabled = 1

- ALE supports running linters or fixers via Docker, virtual machines,
  or in combination with any remote machine with a different file system,
  so long as the tools are well-integrated with ALE  and ALE is properly
  configured to run the correct commands and map filename paths between
  different file systems.

# How can I use ALE and vim-lsp together:
  vim-lsp: "modern" alternative to CoC (pure embedded Lua in neovim).
           including including auto-completion, diagnostics, go-to-definitions, ...

  " ALE will still provide integration of external programs showing LSP errors:
  g:ale_disable_lsp = 1
  vim-lsp-ale bridge-plugin solve any conflict with diagnostics provided by
  vim-lsp and ALE handling all the errors.

- Generating Vim help files:
  " (Put these lines at the very end of your ~/.vimrc file)
  " Load all plugins now.
  " Plugins need to be added to runtimepath before helptags can be generated.
  packloadall
  " Load all of the helptags now, after plugins have been loaded.
  " All messages and errors will be ignored.
  silent! helptags ALL

[[}]]
